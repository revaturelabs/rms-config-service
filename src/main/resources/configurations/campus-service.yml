server:
  port: 10001

spring:
  profiles:
    active: dev
  data:
    mongodb:
      database: rms-campus
      host: localhost
      port: 27017

eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: false
    register-with-eureka: true