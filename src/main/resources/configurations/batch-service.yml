server:
  port: 10003

spring:
  profiles:
    active: dev
  data:
    mongodb:
      database: rms-batch
      host: localhost
      port: 27017

eureka:
  client:
    enabled: true
    fetch-registry: false
    register-with-eureka: true