server:
  port: 10000

spring:
  profiles:
    active: dev
  data:
    mongodb:
      database: rms-users
      host: localhost
      port: 27017

eureka:
  client:
    enabled: true
    fetch-registry: false
    register-with-eureka: true
