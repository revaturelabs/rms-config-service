server:
  port: 10004

spring:
  profiles:
    active: dev
  data:
    mongodb:
      database: rms-work-orders
      host: localhost
      port: 27017

eureka:
  client:
    enabled: true
    fetch-registry: false
    register-with-eureka: true